{
  "name": "@apps/web",
  "tags": ["layer:apps"],
  "targets": {
    "test:unit": {
      "executor": "@nx/vitest:test",
      "options": {
        "config": "apps/web/vitest.config.ts"
      }
    }
  }
}
